import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TableZoneListComponent } from './table-zone-list.component';

describe('TableZoneListComponent', () => {
  let component: TableZoneListComponent;
  let fixture: ComponentFixture<TableZoneListComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ TableZoneListComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(TableZoneListComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
